# Secret key for signing image file names
SECRET_KEY="some secret string of any length"

# Full URL for serving images. It should contain the public hostname of this
# server or the public hostname of the server that will host the images.
IMAGE_SERVING_URL="https://your-server.example.com/images/%FILE_NAME%"
